# v-card-exercice

#Learn Objectives : 

`grid` and `flex` 

#obejctive 

Create a Virtual business card with the `grid` and `flex`. If `grid`are used you can create an another V-card with the `flex`version.

#Used Ressources


